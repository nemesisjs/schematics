import { describe, it, expect, beforeAll } from 'bun:test';
import { <%- pascalName %>Guard } from './<%- kebabName %>.guard';

describe('<%- pascalName %>Guard', () => {
  let guard: <%- pascalName %>Guard;

  beforeAll(() => {
    guard = new <%- pascalName %>Guard();
  });

  it('should be defined', () => {
    expect(guard).toBeDefined();
  });

  it('canActivate() should return true by default', () => {
    expect(guard.canActivate({} as any)).toBe(true);
  });
});
